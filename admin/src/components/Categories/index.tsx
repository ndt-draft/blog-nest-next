import { Button } from "@/components/ui/button";

const Categories = () => (
  <div>
    <h1 className="text-3xl font-bold underline mb-4">Categories</h1>
    <Button className="cursor-pointer">Create New Category</Button>
  </div>
);

export default Categories;
